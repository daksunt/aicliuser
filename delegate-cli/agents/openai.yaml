interface:
  display_name: "Delegate CLI"
  short_description: "Discover, choose, and delegate work to model-powered CLIs."
  default_prompt: "Discover available agent CLIs, ask which to use when needed, then delegate bounded work or review with transcripts and verification."
